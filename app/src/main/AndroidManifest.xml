<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.datadomeapp">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DataDomeAPP">

        <!-- Launcher / Main -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Authentication -->
        <activity android:name=".LoginActivity"/>
        <activity android:name=".ResetPasswordActivity"/>

        <activity android:name=".VerifyEmailActivity"/>
        <activity android:name=".EnterCodeActivity"/>
        <activity android:name=".EnrollmentActivity"/>
        <activity android:name=".AlreadySubmittedActivity"/>

        <!-- Admin Module -->
        <activity android:name=".admin.AdminDashboardActivity"/>
        <activity android:name=".admin.ManageStudentsActivity"/>
        <activity android:name=".admin.ManageTeachersActivity"/>
        <activity android:name=".admin.ManageCoursesActivity"/>
        <activity android:name=".admin.ManageCanteenStaffActivity"/>
        <activity android:name=".admin.ManageEnrollmentsActivity"/>
        <activity android:name=".admin.ManageCurriculumActivity"/>
        <activity android:name=".admin.ManageSchedulesActivity"/>

        <!-- Student Module -->
        <activity android:name=".student.StudentDashboardActivity"/>
        <activity android:name=".UserLibraryActivity"/>
        <activity android:name=".student.StudentScheduleActivity"/>
        <activity android:name=".student.StudentToDoListActivity"/>
        <activity android:name=".student.StudentNotesActivity"/>

        <!-- Teacher Module -->
        <activity android:name=".teacher.TeacherDashboardActivity"/>
        <activity android:name=".teacher.VoiceDetectionActivity"/>

        <!-- Library Module -->
        <activity android:name=".admin.ManageLibrary"/>
        <activity android:name=".BookDetailsActivity"/>

        <!-- Canteen Module -->
        <activity android:name=".canteen.CanteenStaffDashboardActivity"/>
        <activity android:name=".canteen.MenuManagementActivity"/>
        <activity android:name=".canteen.AddEditMenuActivity"/>

    </application>
</manifest>
